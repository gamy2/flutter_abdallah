import 'package:flutter/material.dart';

class ArchPage extends StatelessWidget {
  const ArchPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("arch_page"),);
  }
}